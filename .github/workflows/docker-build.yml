# Docker Hub only (docker.io). Repository Variables (optional):
#   DOCKERHUB_IMAGE_PREFIX — Docker Hub namespace if different from secret username; usually leave unset.
# Secrets: DOCKERHUB_USERNAME, DOCKERHUB_TOKEN
#
# 固定 tag：改下面 env 里的 DOCKER_IMAGE_TAG_FIXED。非空则只推该 tag（+amd64/arm64）及对应 multi-arch；留空 '' 则按分支/semver 自动打标（main→latest 等）。

name: Build and Push Docker Images

on:
  push:
    branches:
      - main
      - dev
      - release/stable
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
      - dev
  workflow_dispatch:

env:
  DOCKER_IMAGE_TAG_FIXED: '20260425'

jobs:
  prepare:
    name: Prepare image name
    runs-on: ubuntu-22.04
    outputs:
      manual_tag: ${{ steps.tag.outputs.manual_tag }}
      has_dockerhub: ${{ steps.creds.outputs.has_dockerhub }}
    steps:
      - name: Detect Docker Hub credentials
        id: creds
        run: |
          if [ -n "${{ secrets.DOCKERHUB_TOKEN }}" ] && [ -n "${{ secrets.DOCKERHUB_USERNAME }}" ]; then
            echo "has_dockerhub=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_dockerhub=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Apply fixed image tag from workflow env
        id: tag
        run: |
          if [ -n "${{ env.DOCKER_IMAGE_TAG_FIXED }}" ]; then
            echo "manual_tag=${{ env.DOCKER_IMAGE_TAG_FIXED }}" >> "$GITHUB_OUTPUT"
            echo "tag mode: manual (DOCKER_IMAGE_TAG_FIXED=${{ env.DOCKER_IMAGE_TAG_FIXED }})"
          else
            echo "manual_tag=" >> "$GITHUB_OUTPUT"
            echo "tag mode: auto (branch/semver/sha)"
          fi

  build-and-push:
    name: Build ${{ matrix.name }} (${{ matrix.arch_tag }})
    needs: prepare
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: backend
            dockerfile: ./docker/Dockerfile.backend
            image_suffix: backend
            platform: linux/amd64
            arch_tag: amd64
            runner: ubuntu-22.04
          - name: backend
            dockerfile: ./docker/Dockerfile.backend
            image_suffix: backend
            platform: linux/arm64
            arch_tag: arm64
            runner: ubuntu-22.04-arm
          - name: frontend
            dockerfile: ./docker/Dockerfile.frontend
            image_suffix: frontend
            platform: linux/amd64
            arch_tag: amd64
            runner: ubuntu-22.04
          - name: frontend
            dockerfile: ./docker/Dockerfile.frontend
            image_suffix: frontend
            platform: linux/arm64
            arch_tag: arm64
            runner: ubuntu-22.04-arm
    steps:
      - name: Resolve Docker Hub image namespace
        id: image
        run: |
          P='${{ vars.DOCKERHUB_IMAGE_PREFIX }}'
          S='${{ secrets.DOCKERHUB_USERNAME }}'
          if [ -z "$P" ]; then
            P="$S"
          fi
          if [ -z "$P" ]; then
            P=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          fi
          {
            echo "image_prefix<<EOF_IMAGE_PREFIX"
            printf '%s\n' "$P"
            echo "EOF_IMAGE_PREFIX"
          } >> "$GITHUB_OUTPUT"
          echo "Resolved image_prefix=$P"

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        if: github.event_name != 'pull_request' && needs.prepare.outputs.has_dockerhub == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata (auto tags)
        if: needs.prepare.outputs.manual_tag == ''
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ steps.image.outputs.image_prefix }}/nofx-${{ matrix.image_suffix }}
          tags: |
            type=ref,event=branch,suffix=-${{ matrix.arch_tag }}
            type=semver,pattern={{version}},suffix=-${{ matrix.arch_tag }}
            type=semver,pattern={{major}}.{{minor}},suffix=-${{ matrix.arch_tag }}
            type=semver,pattern={{major}},suffix=-${{ matrix.arch_tag }}
            type=sha,prefix={{branch}}-,suffix=-${{ matrix.arch_tag }}

      - name: Build and push ${{ matrix.name }}-${{ matrix.arch_tag }} image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: ${{ matrix.platform }}
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ needs.prepare.outputs.manual_tag != '' && format('{0}/nofx-{1}:{2}-{3}', steps.image.outputs.image_prefix, matrix.image_suffix, needs.prepare.outputs.manual_tag, matrix.arch_tag) || steps.meta.outputs.tags }}
          labels: ${{ needs.prepare.outputs.manual_tag == '' && steps.meta.outputs.labels || format('org.opencontainers.image.revision={0}', github.sha) }}
          cache-from: type=gha,scope=${{ matrix.name }}-${{ matrix.arch_tag }}
          cache-to: type=gha,mode=max,scope=${{ matrix.name }}-${{ matrix.arch_tag }}
          build-args: |
            BUILD_DATE=${{ github.event.head_commit.timestamp }}
            VCS_REF=${{ github.sha }}
            VERSION=${{ github.ref_name }}

      - name: Image digest
        run: |
          echo "✅ Built: ${{ matrix.name }}-${{ matrix.arch_tag }}"
          echo "Platform: ${{ matrix.platform }}"

  create-manifest:
    name: Create multi-arch manifests (Docker Hub)
    if: github.event_name != 'pull_request'
    needs: [prepare, build-and-push]
    runs-on: ubuntu-22.04
    permissions:
      contents: read
    strategy:
      matrix:
        image_suffix: [backend, frontend]
    steps:
      - name: Resolve Docker Hub image namespace
        id: image
        run: |
          P='${{ vars.DOCKERHUB_IMAGE_PREFIX }}'
          S='${{ secrets.DOCKERHUB_USERNAME }}'
          if [ -z "$P" ]; then
            P="$S"
          fi
          if [ -z "$P" ]; then
            P=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          fi
          {
            echo "image_prefix<<EOF_IMAGE_PREFIX"
            printf '%s\n' "$P"
            echo "EOF_IMAGE_PREFIX"
          } >> "$GITHUB_OUTPUT"
          echo "Resolved image_prefix=$P"

      - name: Log in to Docker Hub
        if: needs.prepare.outputs.has_dockerhub == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create and push multi-arch manifest
        run: |
          if [[ -z "${{ secrets.DOCKERHUB_TOKEN }}" || -z "${{ secrets.DOCKERHUB_USERNAME }}" ]]; then
            echo "No Docker Hub credentials; skipping manifest."
            exit 0
          fi

          PREFIX='${{ steps.image.outputs.image_prefix }}'
          IMAGE="${PREFIX}/nofx-${{ matrix.image_suffix }}"

          MANUAL='${{ needs.prepare.outputs.manual_tag }}'
          if [[ -n "$MANUAL" ]]; then
            REF_NAME="$MANUAL"
          else
            REF_NAME=$(echo "${{ github.ref_name }}" | sed 's/\//-/g')
          fi

          echo "📦 Docker Hub manifest: ${IMAGE}"
          echo "Ref / tag: ${REF_NAME}"

          docker buildx imagetools create -t "${IMAGE}:${REF_NAME}" \
            "${IMAGE}:${REF_NAME}-amd64" \
            "${IMAGE}:${REF_NAME}-arm64"

          if [[ -n "$MANUAL" ]]; then
            echo "✅ Manual tag (DOCKER_IMAGE_TAG_FIXED): single manifest only."
            echo "🎉 Done"
            exit 0
          fi

          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            docker buildx imagetools create -t "${IMAGE}:latest" \
              "${IMAGE}:${REF_NAME}-amd64" \
              "${IMAGE}:${REF_NAME}-arm64"
            echo "✅ :latest (main)"
          fi

          if [[ "${{ github.ref }}" == "refs/heads/release/stable" ]]; then
            docker buildx imagetools create -t "${IMAGE}:stable" \
              "${IMAGE}:${REF_NAME}-amd64" \
              "${IMAGE}:${REF_NAME}-arm64"
            echo "✅ :stable (release/stable)"
          fi

          echo "🎉 Multi-arch manifest(s) on Docker Hub"
